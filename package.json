{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^18.7.23",
    "lerna": "^5.5.4",
    "typescript": "^4.8.4"
  },
  "scripts": {
	"dev": "cd packages/integration && yarn start",
    "build": "lerna run build",
    "clean": "lerna run clean"
  }
}
