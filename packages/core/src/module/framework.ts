import { IOptions } from '../types/structure';

export default class Framework {
	public constructor(private options?: IOptions) {}

	public async initialise() {
		// todo - initialise the framework
	}

	public async start() {
		// todo - start the framework
	}

	public async stop() {
		// todo - stop the framework
	}
}
